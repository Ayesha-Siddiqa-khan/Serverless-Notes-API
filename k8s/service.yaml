apiVersion: v1
kind: Service
metadata:
  name: serverless-notes-api
spec:
  selector:
    app: serverless-notes-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000
  type: LoadBalancer
